window.rails_routes =
<% Rails.application.routes.routes.each do |route| %>
<% next unless route.name  %>
  <%= route.name %>: "<%= route.path.spec.to_s.gsub(/\(\.:\w+\)/,'').gsub(/:(\w+)/, "{{\\1}}") %>"
<% end %>
